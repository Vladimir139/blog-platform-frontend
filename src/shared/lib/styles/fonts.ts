import { globalCss } from "@stitches/react";

export const setupFonts = globalCss({
  body: {
    fontFamily: "'Inter', sans-serif", // или 'Montserrat, sans-serif' если хотите использовать Montserrat
  },
});
