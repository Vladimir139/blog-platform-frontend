import { styled } from "@/shared/lib/styles";

export const Footer = styled("footer", {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",

  padding: 30,

  marginTop: "auto",
});
